import { IonicMaskDirective } from './ionic-mask.directive';

describe('IonicMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new IonicMaskDirective();
    expect(directive).toBeTruthy();
  });
});
